// updated translations.js content
const translations = {
   // other existing translations...
};

// Remove invalid lines that caused linting issues.
// Note: Line numbers have been specified for clarity, please ensure to keep the existing content intact except the invalid lines.

export default translations;